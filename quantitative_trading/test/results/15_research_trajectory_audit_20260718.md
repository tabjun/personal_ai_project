# 연구 궤적 전수 감사 — 사용자가 짚은 7단계 이탈 경로의 기록 검증 (2026-07-18)

작성일: 2026-07-18 · 브랜치: `stock`

> 이 문서는 사용자가 2026-07-18에 직접 짚은 연구 이탈 경로(아래 원문)를 커밋 이력·보고서·
> 그림 전체와 대조해 검증한 감사다. 이전 감사(`15_direction_audit_10_to_14_20260716.md`)가
> "12번 case 41 기억 검증"에 집중했다면, 이번은 **연구 시작부터의 전체 궤적**과 **꺾인 지점의
> 문서 증거**, 그리고 **원설계(KRW 전 종목 15분봉) 이탈 시점**을 확정한다.

## 0. 사용자가 짚은 궤적 (원문 요지)

1. lagging 문제
2. 최적화 시 '0' 가장 쉬운 해로 수렴하는 문제 + 손실 함수
3. 알고리즘별 파악
4. 최적화와 동시에 예측 변동 자체도 쓰레기. 근데 lagging도 못 잡음
5. 어느샌가 10, 11번쯤 — 최적화·수렴 안정, 예측 변동폭이 넓은데 어느 정도 추세도 잘 따라감
6. 갑자기 꺾어서, 변동 자체가 작아서 뭘 하든 의미가 없는데 "손실(MDD) 수치가 줄었다"는
   결과 수치로만 판단하고 그 위에 변수 추가 실험을 수행
7. 그러다 보니 방향 자체가 조금씩 잘못 감

+ 목록 외 기본: **애초에 실험 설계 때 KRW 전 종목 15분봉 분석**이었는데 왜 단일 종목이 됐나.

사용자 핵심 논점: *"잃지 않는 것은 기본이다. 그런데 예측 변동 자체가 작으면 매매도 안전으로만
가는데, 그 위에서 '방어'라는 게 의미가 있나?"* — 즉 **변동 없는 예측 위에서 측정된 MDD 개선은
성과가 아니다.**

---

## 1. 검증 결과 요약 — 7단계 모두 기록과 일치한다

| 사용자 궤적 | 해당 실험/시기 | 기록 증거 | 일치 여부 |
|---|---|---|---|
| 1. lagging | 1~3번 (2026-05) | 3번 Autoformer "우수" → 2026-06-13 문헌 리뷰에서 가격 레벨 lag-copy 착시로 재해석 | **일치** |
| 2. '0' 쉬운 해 + 손실함수 | 5~6번 (2026-06-13) | 5번 계획서 "shortcut collapse 진단", 6번 "target·loss·selection 안정화" | **일치** |
| 3. 알고리즘별 파악 | 8번 (2026-06-23) | 14개 모델 비교: TimesNet 등 0수익률 collapse(var 0.0025) vs LSTM/GRU/Autoformer 폭주(var 2.3~59) | **일치** |
| 4. 변동 쓰레기 + lagging 못 잡음 | 8~9번 | 8번 "전 모델 persistence 미달", 9번 "전처리 28종으로도 미해결" | **일치** |
| 5. 10·11번 좋았던 지점 | 10~12번 (06-23~24) | 10번 balanced_composite: 학습 곡선 안정 수렴 + huber 대비 변동 보존(진폭 개선), 12번 case 41: Pearson 0.129(전 기록 최고) + KRW 캔들 추세 추종 그림 + 유일한 +수익 | **일치** |
| 6. 꺾인 지점 | **2026-06-24 10·11 종합 보고서** | 아래 2절 상세 | **일치 — 문서로 특정됨** |
| 7. 방향 누적 이탈 | 12→13→14→15계획(06-30) | fusion MDD 단독 랭킹 고착, CLAUDE.md에 "Defense First" 성문화(06-28) | **일치** |
| +. KRW 전 종목 원설계 | 커밋 `c8cee5c`, `b039b8e`, `20e3014` (2026-05-25 전후) | 아래 3절 상세 | **일치 — 이탈 시점 특정됨** |

## 2. 꺾인 지점의 문서 증거 — 2026-06-24, 두 문서

### 2.1 `10_11_point_risk_synthesis_report_20260624.md` (제목부터: "점예측보다 위험 게이트를 먼저 구현해야 한다")

이 보고서는 문제를 **정확히 알고 있었다**:

> "variance ratio도 1보다 크게 작다. **실제 변동 폭의 약 10.8% 수준만 재현**했다는 뜻이므로,
> 이 신호를 전 구간에서 그대로 매매하는 것은 이르다." (3.2절)
> "주황색 예측선은 실제 급등락보다 훨씬 좁고..." (3.3절)

즉 **"예측이 실제 변동의 10.8%만 재현한다"는 사실이 명시돼 있었다.** 그런데 결론은 그 문제를
고치는 것이 아니라:

> "다음 실험은 또 다른 모델·전처리 목록을 늘리는 실험이 아니라, 11번 위험 확률이 낮은
> 구간에서만 10번 점예측을 허용했을 때 **MDD와 거래 성과가 개선되는지** 확인해야 한다."

— **변동 재현 문제(10.8%)를 미해결 과제로 남기는 대신, 그 위에 위험 게이트를 얹어 MDD 수치를
다음 판단 기준으로 채택했다.** 이것이 사용자가 말한 "갑자기 꺾은" 지점이다.

### 2.2 `10_11_to_12_feature_guardrail_transition_report_20260624.md`

> "최적화가 쉬운 해로 붕괴하는 문제는 완전히 해결되지는 않았지만 **다음 최적화 단계로 넘어갈
> 만큼 완화되었다**고 판단할 수 있다."

— 사용자 궤적 6번 그대로다: **미해결(변동 10.8%)이 "완화됨"으로 격하되고, 그 위에 변수 추가
실험(12번 feature ablation)으로 넘어갔다.** 이후 12번의 챔피언 랭킹 지표는 fusion MDD가 됐고,
MDD는 예측이 평평해져도(거래가 줄어도) 나빠지지 않는 지표라 변동 압축을 벌점화하지 않는다.

### 2.3 고착화: 지침 문서에 성문화

- 2026-06-28 `CLAUDE.md` 8장 스냅샷: "**핵심 기조: 다변량 분석, MDD 최소화 우선, Defense First**"
- `AGENTS.md` 보고서 체크리스트: "**MDD 최소화 관점의 종합 결론**" (모든 보고서가 MDD 프레임으로
  결론 내도록 강제)
- 2026-06-30 15번 구계획서: "방어 전용 재설계" (14번의 소규모 아티팩트 위에서)

**즉 한 번의 판단(06-24)이 지침 문서에 박히면서, 이후 모든 세션(사람/AI 불문)이 그 프레임 안에서
계속 일하게 됐다.** 사용자 궤적 7번("조금씩 잘못 감")의 기제가 이것이다.

### 2.4 사용자 논점 검증: "변동 없는 예측 위의 방어는 의미가 있나" → 없다, 기록이 증명

- 12번에서 `no_trade`(아예 거래 안 함)가 MDD 방어 1등으로 둔갑했던 것(2026-06-30 codex 감사
  fix③) 자체가, MDD 단독 랭킹이 "잘 방어함"과 "거의 안 움직임"을 구분하지 못한다는 증거였다.
- 활동 하한(fix③)을 넣어도 근본은 남았다: 예측이 실제 변동의 4~36%만 재현하면(T6 앙상블 0.04,
  15번 우승 0.36) 진입 신호 자체가 드물고 소극적이라, MDD가 좋아 보여도 그것은 "방어를 잘해서"가
  아니라 "애초에 노출이 작아서"다. T5에서 gate가 MDD를 47% 줄였다는 결과도 이 위에서 측정된
  것이므로, **"변동을 제대로 재현하는 예측" 위에서 재측정되기 전까지는 방어 성과로 인정할 수 없다.**

## 3. 원설계 이탈 — "KRW 전 종목 15분봉"은 커밋으로 확정된 출발점이었다

| 커밋 | 내용 |
|---|---|
| `c8cee5c` (2026-05-25경) | "pyupbit 기반 **자동 다중 종목 15분봉** 수집 파이프라인 탑재" |
| `b039b8e` | "**KRW 마켓 필터링** 및 화폐 단위 통일의 퀀트 정당성 명세서 추가" |
| `20e3014` | "**KRW 모든 종목** Naive하게 분석 및 결과 보고서 정리" |
| 3번 실험 (2026-05-25) | 업비트 전체 250여 종목 동적 변동성/거래대금 필터 + 15분봉 |
| 2026-06-08 | historical_flow 마트를 "KRW 마켓 전체 ticker 축"으로 설계, L2 캐시에 "BTC 단일 종목 금지" 명시 |

**이탈 시점**: 4번 텍스트 독립변수 실험(2026-06-09)이 텍스트 mart와의 timestamp 조인 편의로
`btc_15m_advance` 단일 테이블을 쓰면서 시작 → 5번이 그 위에서 최적화 진단(단일 종목 진단은
그 자체로는 합리) → 이후 6~15번, 14개월치 실험 전부가 복귀 없이 BTC 단일 축에서 수행됨.
설계대로 만들어진 `marts/historical_flow.py`(SOURCE_TABLE=`upbit_krw_candle`)는 마트가 한 번도
build되지 않은 채 방치. T8의 "4종목"조차 12번 cross_market 기본값(ETH/XRP/SOL)의 무비판 계승.

**따라서 T8·T9까지의 "예측 신호 없음" 결론은 전부 "BTC 단일 시계열 축에서 없음"으로 한정해야
하며, 원설계 축(KRW 전 종목 15분봉 — 횡단면 포함)은 아직 검증된 적이 없다.**

## 4. 이번에 시행한 교정

1. **데이터 축 복원 (진행 중)**: `pipelines/rebuild_price_mart.py`에 KRW 마켓 전체 모드 추가
   (`--tickers all` → `upbit_krw_candle`, historical_flow SOURCE_TABLE과 동일, resume 지원,
   상장 이전 무한루프 가드). **269종목 × 3년 15분봉 수집이 백그라운드 진행 중.**
2. **지침 문서 교정**: CLAUDE.md 핵심 기조에서 "MDD 최소화 우선, Defense First" 단독 프레임을
   폐기하고, "변동을 제대로 재현·예측하는 모델이 본선, 잃지 않기는 기본 제약(변동 없는 예측
   위에서 측정된 MDD 개선은 성과로 인정하지 않음)"으로 교체. AGENTS.md 보고서 체크리스트의
   "MDD 최소화 관점의 종합 결론"도 "변동 재현(variance_ratio·추세 추종)과 MDD를 함께 보는
   종합 결론"으로 교체. 데이터 축 기본값 = KRW 전 종목 15분봉을 영구 규칙으로 명시.
3. **평가 규칙**: 이후 모든 실험 랭킹에서 MDD 단독 랭킹 금지 — variance_ratio 건강 구간과
   변동 재현율을 반드시 병기하고, 활동 하한과 함께 판정.
4. **작업 방식 (사용자 지시)**: 세션 중간 보고를 남발하지 않고 요청 단위를 끝까지 수행한 뒤
   일괄 보고. `conversation_l2_cache.md`에는 사용자 요청 **원문을 그대로** 보존.

## 4b. 부록 (2026-07-19 추가) — 실험 코드 파일 처분 분류

사용자 질문 "이전에 진행했던 코드는 몇 개 잘못된 건가? 잘못된 코드 파일은 지워야 한다"에 대한
전수 분류. 기준은 **연구 목적(비정상 시계열의 예측 추세 학습) 관점에서 그 코드가 앞으로
재실행될 때 올바른 분석을 재생산하는가**다. "결론이 틀렸던 것"과 "코드 자체가 결함인 것"을
구분한다 — 전자는 감사 문서로 정정하고 이력을 보존하며, 후자만 삭제한다.

| 실험 코드 | 판정 | 근거 |
|---|---|---|
| 1·2·3번 | **보존 (방향 부합)** | 원설계(KRW 전 종목 15분봉) 시대. 3번은 전체 축의 마지막 실험 |
| 4번 | 보존 | 텍스트 독립변수 축 자체는 원설계(Phase 1.2/2.2) 부합. 단 btc 단일화가 여기서 시작(데이터 축 이탈 기점으로 기록) |
| 5~9번 | **보존 (정당한 진단 단계)** | 사용자 궤적 1~4의 문제 규명 실험 — lagging·쉬운 해·알고리즘·전처리 진단은 방향 이탈이 아니라 필요한 과정이었다 |
| 10·11번 | **보존 (좋았던 지점)** | balanced_composite 변동 보존 + 위험확률. 사용자 궤적 5번의 원본 |
| 12번 | **보존 — 단 재실행 금지 (증거 원본)** | 코드에 결함 3종(정렬·텍스트·비활동)이 있어 재실행하면 잘못된 fusion 수치를 재생산하지만, case 41(좋았던 지점의 최고 기록)의 유일한 실행 증거 + 추출 이미지 289장의 원본이라 삭제하면 감사 근거가 사라진다. 코드 기능은 engine/이 교정본으로 대체 완료 |
| **13번** | **삭제 (2026-07-19 실행)** | 결함 12번을 `load_module`로 그대로 체인 + 출력 0(저장 실패) + MDD 프레임 설계. 재실행 시 잘못된 분석만 재생산하고 보존 가치(장애 기록·캡처 결과)는 보고서 4건과 이미지에 이미 있다. git 이력에는 남음 |
| 14번 | 보존 | 드라이버 자체는 교정 엔진 기반 유효 기계장치. "Linear 폭주" 결론만 규모 아티팩트로 정정됨(T9·감사 문서) |
| 15번 | 보존 (현행) | h-step 이탈은 T9로 정정 완료, 코드에 감사 반영 주석 존재. T9 브리지·판독 가이드 등 현행 기계장치 |
| engine/ | 보존 + 경고 추가 | 12 결함 교정본으로 유효. 단 `fusion.show_feature_summary`가 MDD 1차 정렬을 쓰므로 "챔피언 선정용 사용 금지" 경고를 주석·출력에 추가(2026-07-19) |
| 구 15계획서(20260630) | 보존 (문서) | 이탈 증거 문서. 개정판(20260716)이 대체를 명시 |

요약 답변: **코드 파일 기준으로 "잘못된 채 재실행 위험이 있던 것"은 12번(결함 내장, 증거로
보존)과 13번(결함 체인+무결과, 삭제) 2건**이다. 나머지는 결론·해석의 이탈(10_11 종합, 12 전환
보고서, 14 결론, 15 구계획)이었고 이는 코드가 아니라 판단의 문제라 감사 문서로 정정하고
이력을 보존한다.

## 5. 다음 단계 (수집 완료 후)

1. `pipelines/build_historical_flow_mart.py`로 과거 유사국면 full mart build (원설계 Phase 2).
2. KRW 전 종목 축에서 재검증: 단일 종목 시계열로는 존재할 수 없는 **횡단면 신호**(종목 간
   상대 모멘텀·유동성·변동성 순위)를 포함해, "변동 재현 + 추세 추종"을 1차 기준으로 평가.
3. 그 위에서 방어(gate)를 다시 평가 — 변동 있는 예측 위에서만 방어 효과를 인정.
4. LLM 융합(원래 Phase 2~4)은 이 전체 축(과거 유사국면 + 텍스트 + 예측/위험) 위에서 설계.
