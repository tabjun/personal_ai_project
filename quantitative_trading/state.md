# 상태 체크 (State)

목적: **사용자 요청 단위로 "실행됐는지 아닌지"만** 추적한다. 무엇을 왜 했는지의 서술은
`history.md`(이력)와 `conversation_l2_cache.md`(요청 원문+산출물)가 맡고, 이 파일은 그
서술과 무관하게 완료/진행중/미착수 상태만 본다. AI(Claude Code/Codex)가 매 요청마다
직접 갱신한다 — 사용자가 수동으로 체크하는 파일이 아니다.

## 다른 상태 문서와의 구분

- `README.md`: 프로젝트가 무엇인지(전체 설명, 거의 안 바뀜).
- `process.md`: 연구 단계별 To-Do(무엇을 할 것인가, 서술식 `[x]`/`[ ]`, Phase/실험 단위).
- `state.md`(이 파일): 사용자 요청 단위로 "그 요청이 실제로 끝났는가"만 짧게 체크
  (연구 내용 서술 없음, `conversation_l2_cache.md`의 인덱스를 그대로 참조).
- `history.md`: 완료된 작업의 이력 서술(무엇을·왜·어떻게·결과).
- `conversation_l2_cache.md`: 사용자 요청 원문 보존(최근 20개).

## 갱신 규칙

- 사용자가 새 요청을 하면, 그 요청에 대응하는 `conversation_l2_cache.md` 인덱스가 생성된
  직후 이 파일에도 같은 인덱스로 `[ ]` 행을 추가한다(진행중 표시).
- 요청을 완료하면 AI가 그 행을 `[x]`로 바꾸고 완료 날짜를 채운다. 중단/보류 상태면 `[~]`로
  표시하고 비고에 이유를 한 줄 남긴다(예: 사용자 결정 대기, 서버 다운으로 미완).
- `conversation_l2_cache.md`가 20개 트리밍으로 오래된 인덱스를 지워도, 이 파일의 완료
  행은 지우지 않는다(가벼운 체크리스트라 트리밍 대상이 아니다) — 다만 파일이 과도하게
  길어지면 오래된 완료 행을 `history.md`로 흡수하고 여기서는 제거할 수 있다(추후 결정).
- **stock 브랜치 전용.** `develop`/`main`에는 존재하지 않는다 — `AGENTS.md` 2.7절,
  `.githooks/check_repo_policy.py`의 `GOVERNANCE_FILES`에 포함되어 자동 차단된다.

| 인덱스 | 요청 요약 | 상태 | 완료일 | 비고 |
| :--- | :--- | :--- | :--- | :--- |
| 101 | state.md 신설: 요청 단위 완료 여부를 AI가 체크하는 상태 파일 추가, stock 전용, 문서 역할 지침 갱신 | [x] | 2026-08-09 | `state.md` 신설, `AGENTS.md`에 문서 역할 절 추가, `check_repo_policy.py` GOVERNANCE_FILES에 `state.md` 추가. |
| 102 | 연구 재개 지점 확인(/test 연구 쪽에서 하려던 것) | [x] | 2026-08-10 | 답변만 수행(파일 변경 없음). 대기 결정 3덩어리 + 잔여 작업 2건 식별. |
| 103 | MCP(arxiv·HF) 점검·구축 → 잔여 작업 2건 수행 → 문서 전체 갱신 → 일괄 커밋/푸시 | [x] | 2026-08-10 | MCP 2종 신규 구축, s10~s12 재현 코드 복원 + s13 신규, 보고서 1건, 문서 5종 갱신. 잔여 작업 2건 모두 종료. |
| 104 | 차분 여부가 분석 방향을 바꾸는 이유 시각화·EDA + GARCH 판정 기준 + 전처리 미문서화 해소 | [x] | 2026-08-14 | 18번 드라이버·그림 7장·보고서 완료. 전처리 계보 D0 문서화. GARCH 관문을 파라미터→표본외 성능으로 교체. 부수 교정 3건(다종목 가드, 드라이버 10개 기본값, 캐시 이물 태그). git-lfs 설치. 미완: 교수님 브리프 정정 2건, 15·16·17번 재실행. |
| 105 | 코드 재실행(교정 축에서 핵심 실험 재실행) + 반감기 개념 질문 | [x] | 2026-08-14 | 17·16·15번 재실행 완료, 결론 전부 유지. 발견 2건: 15번 승계 구성 명시 필요, t8 trend_corr 잡음>신호. 7월 산출물 복원 + 8월 검증 디렉터리 분리. 미완: t8 다중 시드 재실행, 교수님 브리프 정정 2건. |
| 106 | 다중시드 진행 + 정정보고 내용 먼저 보고 + 전체 종합보고서(시각화 포함) + NVML 패키지 업그레이드 검토 | [x] | 2026-08-14 | 다중시드 5회로 잡음 정량화(SNR 0.58, 부호 뒤집힘 7/10, GPU 비결정성 0). NVML은 패키지로 해결 불가(재부팅 필요) 확인 후 gpu_status.py 대체 도구 신설. MASTER_REPORT_20260814.md로 전체 통합. 브리프 정정 3건은 초안만 작성 — **사용자 검토 대기**. |
| 107 | 설명 부분까지 전부 대조 시각화 + 지표 유효성 전수 검증 + DB 자동구축 + 용어 흐름 재작성 + NVML 처리 주체 확인 (이전 요청 미답분 합산) | [x] | 2026-08-19 | `MASTER_REPORT_20260819.md`(v2, 그림 10장·용어사전·18절) 신설. 설명용 그림 6장(18e), 전처리 사다리(18d), 지표 유효성 매트릭스(18f), `engine/preflight.py` DB 가드 신설. v1 정정 7건 명시. 메일 발송은 보류(요청). |
| 108 | 새로 나온 항목 전부 분석 수행 후 종합 보고 | [x] | 2026-08-19 | 19번(t분포 GARCH·주기제거·horizon 삼원격자)으로 B 갈래 모델 확정(4h GARCH-t / 16h HAR-RV), 주기 제거가 18셀 전부 개선·반감기 54배 왜곡 규명. 19b 생존편의(폐지 2건 확인, 크기 측정 불가). 17b 브리프 4종 재현(2종은 정의 인공물 판정). 보고서 `19_new_findings_report_20260819.md`. 브리프 정정 5건+1로 확대, 발송 계속 보류. |
| 109 | R²·112% 표기 오류 지적 + preflight BTC예시 지적(전종목 분석 원함) | [x] | 2026-08-19 | R² 112%는 지표값이 아니라 분리도(구별력 지표)임을 각주로 정정, R² 실측은 -0.000055/+0.000461로 둘 다 0 근처. 18d·18e·18f는 개념설명용이라 BTC 단일 유지가 맞으나 19번(모델선택)은 실제 결함 — 19c 신설해 20종목 확인, h=64에서 GARCH-t/HAR-RV 동률(7/20씩) 발견해 'HAR-RV 확정' 결론을 '두 모델 후보'로 정정. AGENTS.md에 preflight 사용원칙(예시=BTC가 항상 BTC만 확인 의미 아님) 보강. |
| 110 | 방향이 어긋났다는 지적 → 지금까지 전부 한 장 정리 후 연구 중단 | [x] | 2026-08-21 | `SUMMARY_ONEPAGE_20260821.md` 신설. A/B/C 결정 전까지 신규 실험 중단, 19~21 태스크 보류. |
| 111 | 교수님 방향 제안 보고서(변동성 추천) + 메일 초안 작성 | [x] | 2026-08-21 | `professor_direction_recommendation_20260821.md` 신설, 문헌 10편(6편 원문 검증 인용), (a)/(b) 프레이밍 포함. 메일 본문 초안 제시, 발송은 사용자 확인 대기(미발송). |
